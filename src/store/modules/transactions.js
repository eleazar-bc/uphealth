// import firebase from 'firebase/app';
import {db} from '../../utils/db';

const state = {
    sales: []
}

const getters = {};

const actions = {
    
};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
}